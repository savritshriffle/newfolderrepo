import { CDirectiveDirective } from './c-directive.directive';

describe('CDirectiveDirective', () => {
  it('should create an instance', () => {

    const directive = new CDirectiveDirective()

    expect(directive).toBeTruthy();
  });
});
